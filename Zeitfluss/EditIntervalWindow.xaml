<Window x:Class="Zeitfluss.EditIntervalWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Arbeitszeit korrigieren" Width="500" Height="530" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Background="#F4F4F6" FontFamily="Segoe UI Variable Text">
    <Grid Margin="30">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Arbeitszeit korrigieren" FontFamily="Segoe UI Variable Display" FontSize="27" FontWeight="SemiBold" />
            <TextBlock Text="Beginn, Ende und Rundung dieses Eintrags anpassen." Foreground="{StaticResource MutedInk}" Margin="0,6,0,0" />
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="16" Padding="20" Margin="0,24,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="16" /><ColumnDefinition /></Grid.ColumnDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0">
                    <TextBlock Text="Beginn · Datum" FontSize="11" Foreground="{StaticResource MutedInk}" Margin="2,0,0,6" />
                    <TextBox x:Name="StartDateTextBox" />
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="2">
                    <TextBlock Text="Beginn · Uhrzeit" FontSize="11" Foreground="{StaticResource MutedInk}" Margin="2,0,0,6" />
                    <TextBox x:Name="StartTimeTextBox" />
                </StackPanel>

                <StackPanel x:Name="EndDatePanel" Grid.Row="1" Grid.Column="0" Margin="0,18,0,0">
                    <TextBlock Text="Ende · Datum" FontSize="11" Foreground="{StaticResource MutedInk}" Margin="2,0,0,6" />
                    <TextBox x:Name="EndDateTextBox" />
                </StackPanel>
                <StackPanel x:Name="EndTimePanel" Grid.Row="1" Grid.Column="2" Margin="0,18,0,0">
                    <TextBlock Text="Ende · Uhrzeit" FontSize="11" Foreground="{StaticResource MutedInk}" Margin="2,0,0,6" />
                    <TextBox x:Name="EndTimeTextBox" />
                </StackPanel>

                <CheckBox x:Name="OngoingCheckBox" Grid.Row="2" Grid.ColumnSpan="3" Content="Erfassung läuft weiter (kein Ende)" Margin="2,18,0,0" Checked="Ongoing_Changed" Unchecked="Ongoing_Changed" />
                <TextBlock x:Name="RoundingInfoText" Grid.Row="3" Grid.ColumnSpan="3" Foreground="{StaticResource MutedInk}" FontSize="12" TextWrapping="Wrap" Margin="2,14,0,0" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Margin="2,14,2,0">
            <TextBlock Text="Datum: TT.MM.JJJJ · Uhrzeit: HH:MM oder HH:MM:SS" FontSize="11" Foreground="{StaticResource MutedInk}" />
            <TextBlock x:Name="ErrorText" TextWrapping="Wrap" FontSize="12" Foreground="{StaticResource Negative}" Margin="0,8,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource GhostButton}" Content="Abbrechen" Margin="0,0,8,0" Click="Cancel_Click" />
            <Button Style="{StaticResource PrimaryButton}" Content="Änderung speichern" Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>
